info:
  name: CreateUSer
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/user/create"
  body:
    type: json
    data: |-
      {
        "username": "pedro",
        "password": "pedro",
        "email": "me@mpps.qzz.io"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
